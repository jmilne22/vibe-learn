## Preprocessor

### Topics Covered

- #include and header search paths
- #define for constants and macros
- Macros with arguments and variadic macros
- Macro pitfalls: side effects, operator precedence
- Conditional compilation: #if, #ifdef, #ifndef, #elif, #else, #endif
- Include guards and #pragma once
- Predefined macros (__FILE__, __LINE__, __func__)
- Stringification (#) and token pasting (##)

---

*Content coming soon.* This module will cover the C preprocessor — a text-substitution engine that runs before compilation. It covers macros, conditional compilation, and the subtle bugs that macros can introduce.
